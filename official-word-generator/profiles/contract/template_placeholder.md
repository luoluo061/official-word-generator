# Template Placeholder

No approved contract Word template is bundled yet.

Before this profile is used for production, add one of:

- `template.docx` in this directory, or
- a stable path in `features.json` pointing to the approved contract template.

For user-uploaded contract templates, inspect the template first and treat it as a custom profile until it is promoted into this directory.
