# Template

This profile currently uses the shared base template:

`../../assets/base-official-template.docx`

Do not duplicate the template here unless this profile later needs its own independent Word template.
