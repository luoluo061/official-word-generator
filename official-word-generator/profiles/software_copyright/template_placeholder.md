# Template Placeholder

No official software copyright Word template is bundled yet.

Before this profile is used for production, add one of:

- `template.docx` in this directory, or
- a stable path in `features.json` pointing to the approved template.

Do not use the general official document template as a final software copyright template unless the user explicitly accepts that limitation.
